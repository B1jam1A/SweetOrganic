extend layout 

block content
  h1= title
  p Welcome to #{title}
    ul 
        each article in articlesList 
            li 
                h2= article.idArticle 
                p Quantité : #{article.qty}
                p Prix : #{getArticlePrice(article)}
    
    p Prix Total : #{getTotalPrice(articlesList)}
